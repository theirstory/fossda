{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "skipAutoInstall": true,
        "buildCommand": "npm run typecheck && next build"
      }
    }
  ]
} 